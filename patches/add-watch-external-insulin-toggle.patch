diff --git a/Loop/Common/Models/SetBolusUserInfo.swift b/Common/Models/SetBolusUserInfo.swift
index a12f7c2e..c25f1793 100644
--- a/Loop/Common/Models/SetBolusUserInfo.swift
+++ b/Loop/Common/Models/SetBolusUserInfo.swift
@@ -16,6 +16,8 @@ struct SetBolusUserInfo {
     let contextDate: Date?
     let carbEntry: NewCarbEntry?
     let activationType: BolusActivationType
+    let isExternalInsulin: Bool
+    let insulinType: InsulinType?
 }
 
 
@@ -41,6 +43,8 @@ extension SetBolusUserInfo: RawRepresentable {
         self.contextDate = rawValue["cd"] as? Date
         self.carbEntry = (rawValue["ce"] as? NewCarbEntry.RawValue).flatMap(NewCarbEntry.init(rawValue:))
         self.activationType = activationType
+        self.isExternalInsulin = rawValue["ei"] as? Bool ?? false
+        self.insulinType = (rawValue["it"] as? InsulinType.RawValue).flatMap(InsulinType.init(rawValue:))
     }
 
     var rawValue: RawValue {
@@ -54,6 +58,8 @@ extension SetBolusUserInfo: RawRepresentable {
         raw["cd"] = contextDate
         raw["ce"] = carbEntry?.rawValue
         raw["at"] = activationType.rawValue
+        raw["ei"] = isExternalInsulin
+        raw["it"] = insulinType?.rawValue
 
         return raw
     }

diff --git a/Loop/Loop/Managers/WatchDataManager.swift b/Loop/Managers/WatchDataManager.swift
index bac60b71..21a8ed22 100644
--- a/Loop/Loop/Managers/WatchDataManager.swift
+++ b/Loop/Loop/Managers/WatchDataManager.swift
@@ -384,15 +384,24 @@ final class WatchDataManager: NSObject {
                 return
             }
 
-            deviceManager.enactBolus(units: bolus.value, activationType: bolus.activationType) { (error) in
-                if error == nil {
-                    self.deviceManager.analyticsServicesManager.didBolus(source: "Watch", units: bolus.value)
-                }
-
-                // When we've successfully started the bolus, send a new context with our new prediction
+            // Handle external insulin (from pen) vs pump bolus
+            if bolus.isExternalInsulin {
+                // Log as manually entered dose
+                let insulinType = bolus.insulinType ?? deviceManager.pumpManager?.status.insulinType ?? .novolog
+                deviceManager.loopManager.addManuallyEnteredDose(startDate: bolus.startDate, units: bolus.value, insulinType: insulinType)
                 self.sendWatchContextIfNeeded()
+            } else {
+                // Deliver via pump
+                deviceManager.enactBolus(units: bolus.value, activationType: bolus.activationType) { (error) in
+                    if error == nil {
+                        self.deviceManager.analyticsServicesManager.didBolus(source: "Watch", units: bolus.value)
+                    }
+
+                    // When we've successfully started the bolus, send a new context with our new prediction
+                    self.sendWatchContextIfNeeded()
 
-                self.deviceManager.loopManager.updateRemoteRecommendation()
+                    self.deviceManager.loopManager.updateRemoteRecommendation()
+                }
             }
         }
 
diff --git a/Loop/WatchApp Extension/View Models/CarbAndBolusFlowViewModel.swift b/WatchApp Extension/View Models/CarbAndBolusFlowViewModel.swift
index 0cc21ca5..467a1a60 100644
--- a/Loop/WatchApp Extension/View Models/CarbAndBolusFlowViewModel.swift
+++ b/Loop/WatchApp Extension/View Models/CarbAndBolusFlowViewModel.swift
@@ -26,6 +26,7 @@ final class CarbAndBolusFlowViewModel: ObservableObject {
     @Published var recommendedBolusAmount: Double?
     @Published var bolusPickerValues: BolusPickerValues
     @Published var error: Error?
+    @Published var isExternalInsulin: Bool = false
 
     // MARK: - Other state
     let interactionStartDate = Date()
@@ -208,7 +209,7 @@ final class CarbAndBolusFlowViewModel: ObservableObject {
         }
         self.hasSentConfirmationMessage = true
 
-        let bolus = SetBolusUserInfo(value: bolus, startDate: Date(), contextDate: self.contextDate, carbEntry: carbEntry, activationType: .activationTypeFor(recommendedAmount: recommendedBolusAmount, bolusAmount: bolus))
+        let bolus = SetBolusUserInfo(value: bolus, startDate: Date(), contextDate: self.contextDate, carbEntry: carbEntry, activationType: .activationTypeFor(recommendedAmount: recommendedBolusAmount, bolusAmount: bolus), isExternalInsulin: isExternalInsulin, insulinType: nil)
         do {
             try WCSession.default.sendBolusMessage(bolus) { [weak self] (error) in
                 DispatchQueue.main.async {
diff --git a/Loop/WatchApp Extension/Views/Carb Entry & Bolus/BolusInput.swift b/WatchApp Extension/Views/Carb Entry & Bolus/BolusInput.swift
index 4c01e5c4..9615144c 100644
--- a/Loop/WatchApp Extension/Views/Carb Entry & Bolus/BolusInput.swift
+++ b/Loop/WatchApp Extension/Views/Carb Entry & Bolus/BolusInput.swift
@@ -13,6 +13,7 @@ import LoopKit
 
 struct BolusInput: View {
     @Binding var amount: Double
+    @Binding var isExternalInsulin: Bool
     var isComputingRecommendedAmount: Bool
     var recommendedAmount: Double?
     var pickerValues: BolusPickerValues
@@ -37,6 +38,8 @@ struct BolusInput: View {
 
     var body: some View {
         VStack(spacing: 0) {
+            deliveryModeToggle
+
             DoseVolumeInput(
                 volume: amount,
                 isEditable: isEditable,
@@ -51,11 +54,30 @@ struct BolusInput: View {
                 rotationsPerIncrement: 1/24
             )
 
-            if isEditable {
+            if isEditable && !isExternalInsulin {
                 recommendedAmountLabel
             }
         }
     }
+
+    private var deliveryModeToggle: some View {
+        HStack(spacing: 8) {
+            Button(action: { isExternalInsulin = false }) {
+                Image(systemName: "drop.fill")
+                    .font(.system(size: 20))
+                    .foregroundColor(isExternalInsulin ? .gray : .insulin)
+            }
+            .buttonStyle(PlainButtonStyle())
+
+            Button(action: { isExternalInsulin = true }) {
+                Image(systemName: "pencil")
+                    .font(.system(size: 20))
+                    .foregroundColor(isExternalInsulin ? .insulin : .gray)
+            }
+            .buttonStyle(PlainButtonStyle())
+        }
+        .padding(.bottom, 4)
+    }
 
     private var recommendedAmountLabel: some View {
         recommendedAmountLabelText
diff --git a/Loop/WatchApp Extension/Views/Carb Entry & Bolus/CarbAndBolusFlow.swift b/WatchApp Extension/Views/Carb Entry & Bolus/CarbAndBolusFlow.swift
index 0c27958f..7fd5484f 100644
--- a/Loop/WatchApp Extension/Views/Carb Entry & Bolus/CarbAndBolusFlow.swift
+++ b/Loop/WatchApp Extension/Views/Carb Entry & Bolus/CarbAndBolusFlow.swift
@@ -110,6 +110,7 @@ extension CarbAndBolusFlow {
             } else {
                 BolusInput(
                     amount: $bolusAmount,
+                    isExternalInsulin: $viewModel.isExternalInsulin,
                     isComputingRecommendedAmount: viewModel.isComputingRecommendedBolus,
                     recommendedAmount: viewModel.recommendedBolusAmount,
                     pickerValues: viewModel.bolusPickerValues,
